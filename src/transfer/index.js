// =============================================
// RemoteLink - File Transfer Module
// Handles file sending and receiving between peers
// =============================================

class FileTransfer {
  constructor() {
    this.transfers = [];
  }

  sendFile(filePath, peer) {
    // TODO: Send file to connected peer
  }

  receiveFile(data, savePath) {
    // TODO: Receive file from connected peer
  }
}

module.exports = { FileTransfer };
